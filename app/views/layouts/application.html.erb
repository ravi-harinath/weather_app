<!DOCTYPE html>
<html>
  <head>
    <title>AccuWeather Forecast</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_include_tag 'application', defer: true %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <div class="container mx-auto p-6">
      <header class="mb-6">
        <h1 class="text-3xl font-bold">AccuWeather Forecast</h1>
        <p class="text-sm text-gray-600">Enter a postal code or address to get the current conditions.</p>
      </header>

      <% if flash[:alert] %>
        <div class="bg-red-100 border border-red-300 text-red-800 p-3 rounded mb-4"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
